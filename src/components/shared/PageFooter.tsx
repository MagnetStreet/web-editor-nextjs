import { Box } from '@mui/material';
import * as React from 'react';

const PageFooter = () => {
  return (
    <section>
      <Box sx={{ m: 2, textAlign: 'center', fontSize: '0.8rem' }}>
        Some Footer for sample
      </Box>
    </section>
  );
};

export default PageFooter;
